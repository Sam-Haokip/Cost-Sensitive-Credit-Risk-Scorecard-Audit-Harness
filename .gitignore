# raw & processed data (large, not for git)
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep
*.csv
*.csv.gz
*.parquet

# credentials
kaggle.json
.env

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/

# README figures are regenerated by reports/make_figures.py but committed,
# so the README renders on GitHub without running anything.
